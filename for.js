const n = 20;

for(let i=0 ; i<10 ; i++){
    console.log(i);
}
//console.log(i);--undefined

//JSON : Java Script Object Notation
// java, rust, python should use json in some cases
// key - value
// key should be always a string and value can be anything
const json = {
    name: 'rima',
    surname: 'sidique',
    chocolates: a,
}
console.log(json.chocolates);
